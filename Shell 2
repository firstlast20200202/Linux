$ ls -lh <file-name.faile-type>        //shows up the size of the file 

$ tar -cf test.tar file1 file2 file3
$ ls -ltr test.tar
$ tar -tf test.tar
./file1
./file2
./file3
$ tar -zcf test.tar file2 file2 file3        //compress

3 ways of compressing and uncompressing
$ bzip2 test.img           //compress            $ bunzip2 test.img.bz2           //uncompress
$ du -sh test.img.bz2      //verify              $ du -sh test.img                //verify

$ gzip test.img            //compress            $ gunzip test.img.gz             //uncompress     
$ du -sh test.img.gz       //verify              $ du -sh test.img                //verify

$ xz test.img              //compress            $ unxz test.img.xz               //uncompress
$ du -sh test.img.xz       //verify              $ du -sh test.img                //verify

read a compressed file without uncompressing it: zcat/bzcat/xzcat
$ zcat test.txt.bz2


search files:
$ locate file.txt                                    //this command depends on DB "molocate.db", so make sure to keep it updated
$ updatedb
$ find /home/lily -name file.txt                     //find a file named "file.txt" under this folder /home/lily

$ grep word file.txt                         
$ grep -i word file.txt                              //not case sensitive
This line inlcudes WORD.                             //output        
$ grep -r "words here" /home/lily                    //search a word under folder /home/lily
./sample.txt: This line inlcudes words here.         //output file name and the word searched
$ grep -V "word" file.txt                            //prints all the lines which dont inlcude "word"















